import { Navbar } from '@/components/common/Navbar'

/**
 * MainNavigationSection
 * Displays the main navigation bar from the navbar component.
 * This section should remain in the sections folder for now as it's
 * still homepage-specific rendering.
 */
export function MainNavigationSection() {
  return <Navbar />
}